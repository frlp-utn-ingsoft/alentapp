
generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client"
}

datasource db {
  provider = "postgresql"
}


enum MemberCategory {
  Pleno
  Cadete
  Honorario
}

enum MemberStatus {
  Activo
  Moroso
  Suspendido
}

<<<<<<< HEAD
enum PaymentStatus {
  Pending
  Paid
  Canceled
  Overdue
}


model Member {
  id         String         @id @default(uuid()) @db.Uuid 
  dni        String         @unique
  name       String
  email      String         @unique
  birthdate  DateTime?      @db.Date
  category   MemberCategory
  status     MemberStatus   @default(Activo)
  created_at DateTime       @default(now())
  
  // Relación uno a muchos hacia pagos
  payments   Payment[]

  @@map("members")
}


model Payment {
  id        String        @id @default(uuid()) @db.Uuid
  amount    Float
  month     Int
  year      Int
  dueDate   DateTime      @map("due_date")
  
  status    PaymentStatus @default(Pending)
  
  memberId  String        @map("member_id") @db.Uuid

  member    Member        @relation(fields: [memberId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([memberId, month, year])
  
  @@map("payments")
=======
enum EquipmentLoanStatusEnum {
    Loaned
    Returned
    Damaged
}

model Member {
    id         String         @id @default(uuid())
    dni        String         @unique
    name       String
    email      String         @unique
    birthdate  DateTime?      @db.Date
    category   MemberCategory
    status     MemberStatus   @default(Activo)
    created_at DateTime       @default(now())

    equipment_loans EquipmentLoan[]

    @@map("members") // Para que en la DB la tabla se llame 'members' en minúscula
>>>>>>> 62c409102443ef9aa5ceb24f821f9c13bee12ac5
}

model EquipmentLoan {
    id         String         @id @default(uuid())
    item_name  String
    status     EquipmentLoanStatusEnum @default(Loaned)
    loan_date  DateTime       @default(now())
    due_date   DateTime
    member_id  String
    member     Member         @relation(fields: [member_id], references: [id])

    @@map("equipment_loans")
}

model Sport {
    id                           String  @id @default(uuid())
    name                         String  @unique
    description                  String
    max_capacity                 Int
    additional_price             Decimal @db.Decimal(10, 2)
    requires_medical_certificate Boolean

    @@map("sports")
}
